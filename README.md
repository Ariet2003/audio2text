# Telegram Bot для управления задачами через аудио

Профессиональный Telegram бот на Aiogram для добавления задач через голосовые сообщения с интеграцией Google Sheets.

## Возможности

- 🎤 Распознавание речи из аудио файлов (русский язык)
- ➕ Добавление задач в Google Sheets через голосовые сообщения
- 📋 Просмотр всех задач из Google Sheets
- 🔗 Прямая ссылка на Google Sheet
- 🔄 Автоматическая конвертация аудио форматов (OGG, MP3, M4A → WAV)
- ⏱️ Автоматическое добавление даты и времени к каждой задаче
- 🎯 FSM (Finite State Machine) для управления состояниями диалога
- 🧹 Автоматическая очистка временных файлов

## Установка

1. Клонируйте репозиторий или скачайте файлы проекта

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Установите дополнительные зависимости для работы с аудио:
```bash
# Для macOS
brew install portaudio ffmpeg

# Для Ubuntu/Debian
sudo apt-get install portaudio19-dev python3-pyaudio ffmpeg

# Для Windows
# 1. Скачайте и установите PyAudio wheel с https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
# 2. Скачайте и установите ffmpeg с https://ffmpeg.org/download.html
#    Добавьте ffmpeg в PATH
```

## Настройка

### 1. Telegram Bot

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота

### 2. Google Sheets API

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google Sheets API и Google Drive API
4. Создайте Service Account:
   - Перейдите в "IAM & Admin" → "Service Accounts"
   - Создайте новый Service Account
   - Скачайте JSON файл с ключами (credentials.json)
5. Создайте Google Sheet и поделитесь им с email из Service Account (дайте права редактора)
6. Скопируйте ID Sheet из URL: `https://docs.google.com/spreadsheets/d/SHEET_ID/edit`

### 3. Конфигурация

1. Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Заполните `.env` файл:
```
BOT_TOKEN=ваш_токен_бота
GOOGLE_SHEETS_CREDENTIALS_FILE=credentials.json
GOOGLE_SHEET_ID=ваш_sheet_id
GOOGLE_SHEET_NAME=Tasks
GOOGLE_SHEET_LINK=https://docs.google.com/spreadsheets/d/ваш_sheet_id/edit
```

3. Поместите `credentials.json` в корневую директорию проекта

## Запуск

```bash
python bot.py
```

## Структура проекта

```
audio2text/
├── bot.py                 # Основной файл бота
├── config.py              # Конфигурация
├── audio_recognizer.py    # Модуль распознавания речи
├── google_sheets.py       # Модуль работы с Google Sheets
├── main.py                # Оригинальный файл распознавания
├── requirements.txt       # Зависимости
├── .env                   # Переменные окружения (не в git)
├── .env.example           # Пример конфигурации
├── credentials.json       # Google Service Account ключи (не в git)
└── README.md              # Документация
```

## Использование

### Основные команды

1. Запустите бота командой `/start`
2. Используйте кнопки меню:
   - **➕ Добавить задачу** - отправьте голосовое сообщение или аудио файл
   - **📋 Посмотреть задачи** - просмотрите все задачи из Google Sheet
   - **🔗 Ссылка на Google sheet** - получите прямую ссылку на таблицу

### Примеры использования

#### Добавление задачи через голосовое сообщение

1. Нажмите кнопку "➕ Добавить задачу"
2. Запишите голосовое сообщение, например: "Купить молоко и хлеб"
3. Отправьте голосовое сообщение боту
4. Бот распознает речь и добавит задачу в Google Sheet с текущей датой и временем

#### Просмотр всех задач

1. Нажмите кнопку "📋 Посмотреть задачи"
2. Бот отобразит список всех задач с датами и временем создания

#### Получение ссылки на таблицу

1. Нажмите кнопку "🔗 Ссылка на Google sheet"
2. Бот отправит прямую ссылку на вашу Google таблицу

### Поддерживаемые форматы аудио

Бот автоматически обрабатывает следующие форматы:
- Voice messages (OGG)
- MP3
- M4A
- WAV

Все форматы автоматически конвертируются в оптимальный формат для распознавания речи.

## Технологии

- **Aiogram 3.3** - современный асинхронный фреймворк для Telegram ботов
- **SpeechRecognition** - библиотека для распознавания речи через Google Speech API
- **gspread** - библиотека для работы с Google Sheets API
- **python-dotenv** - управление переменными окружения
- **pydub** - конвертация аудио форматов
- **google-auth** - аутентификация в Google API

## Архитектура проекта

### Модули

#### bot.py
Основной модуль бота, содержит:
- Инициализацию бота и диспетчера
- Обработчики команд и кнопок
- FSM состояния для управления диалогом
- Логику обработки аудио файлов

#### audio_recognizer.py
Модуль распознавания речи:
- Класс `AudioRecognizer` для работы с аудио
- Автоматическая конвертация форматов
- Интеграция с Google Speech Recognition API
- Оптимизация аудио (моно, 16kHz)

#### google_sheets.py
Модуль работы с Google Sheets:
- Класс `GoogleSheetsManager` для управления таблицей
- Автоматическое создание листа и заголовков
- Добавление задач с датой и временем
- Форматирование задач для вывода

#### config.py
Конфигурационный модуль:
- Загрузка переменных окружения
- Централизованное хранение настроек

### Структура Google Sheet

Таблица содержит три колонки:
| Дата | Время | Задача |
|------|-------|--------|
| 2026-03-18 | 14:30:25 | Купить молоко |
| 2026-03-18 | 15:45:10 | Позвонить клиенту |

## Устранение неполадок

### Бот не распознает речь

- Убедитесь, что аудио записано четко и без сильного фонового шума
- Проверьте, что установлен ffmpeg: `ffmpeg -version`
- Попробуйте записать более длинное сообщение (минимум 1-2 секунды)

### Ошибка подключения к Google Sheets

- Проверьте, что файл `credentials.json` находится в корневой директории
- Убедитесь, что Google Sheet расшарен с email из Service Account
- Проверьте правильность `GOOGLE_SHEET_ID` в `.env`
- Убедитесь, что включены Google Sheets API и Google Drive API в Google Cloud Console

### Ошибка "Module not found"

```bash
# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### Проблемы с PyAudio на macOS

```bash
# Установите portaudio через Homebrew
brew install portaudio

# Затем установите PyAudio
pip install pyaudio
```

### Бот не отвечает

- Проверьте, что бот запущен: `ps aux | grep bot.py`
- Проверьте правильность `BOT_TOKEN` в `.env`
- Убедитесь, что бот не заблокирован в Telegram

## Требования к системе

- Python 3.8 или выше
- Интернет-соединение для работы с Google API и Telegram API
- ffmpeg для конвертации аудио
- Минимум 100 МБ свободного места для временных файлов

## Безопасность

- Никогда не коммитьте файлы `.env` и `credentials.json` в git
- Используйте `.gitignore` для исключения конфиденциальных файлов
- Регулярно обновляйте зависимости: `pip install -r requirements.txt --upgrade`
- Ограничьте права Service Account только необходимыми API

## Дальнейшее развитие

Возможные улучшения:
- Добавление поддержки других языков
- Редактирование и удаление задач
- Установка напоминаний
- Категоризация задач
- Экспорт задач в другие форматы
- Поддержка нескольких пользователей с отдельными таблицами

## Вклад в проект

Если вы хотите внести свой вклад:
1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения и протестируйте
4. Создайте Pull Request

## Лицензия

MIT
